/* Edit the array as you wish. It will contain the typed strings. */
var config_typedArray = ["are devoted designers.", "develop creative things."];

/* Enable the Typed effect */
var config_enableTyped = false;

/* Disable Parallax effect on mobile/touch devices for performance reasons. */
var config_disableParalaxOnTouchDevices = true;

/* Front page effect - can be 'none', 'particles', 'video'  */
var config_frontPageEffect = 'particles';

/* If config_frontPageEffect is set to video, here set the video URL, type and opacity */
var config_videoUrl = 'video/background.mp4';
var config_videoType = 'video/mp4';
var config_videoOpacity = 1;

/* Google map parameters */
var config_mapLat = 51.509036; // latitude
var config_mapLng = -0.123280; // longitude
var config_mapZoom = 16; // map zoom
var config_mapTitle = 'My company' // marker title